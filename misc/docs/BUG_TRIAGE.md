# Bug Triage - project config and state

Game-specific configuration and running state for the `game-bug-triage` skill.
The skill itself is game-independent; everything project-specific lives here.
Read this file at the start of every triage run.

## Game reference

- **BGA game id:** 2758 (Fate: Defenders of Grimheim)
- **Bug list:** https://boardgamearena.com/bugs?game=2758
- **Single report:** https://boardgamearena.com/bug?id=<REPORT_ID>
- **Studio error log:** https://studio.boardgamearena.com/studioissues?game=fate - studio short name is `fate`, not the numeric id above. Add `&tableId=<TABLE_ID>` for one table, `&id=<ERROR_ID>` for one error, `&projects=frontend|backend` to filter. NOTE: short name not yet verified against the server.

## Rules sources (for `rules`-type reports)

Check a reporter's claim against these, in order, before assuming the code is wrong:

1. [RULES.md](RULES.md) - the rulebook - you must read this no matter what you doing.
2. [FORUM.md](FORUM.md) - designer clarifications and rulings saved from the forum.

## Deploy model

Process: **creating a git tag = deployed.** To decide if a fix is live:

- Find the fix commit: `git log --oneline -i --grep='#<reportid>'` (commit messages reference `BGA #<id>`).
- Is it in the latest tag: `git merge-base --is-ancestor <commit> <latest-tag> && echo LIVE`.
- List tags containing it: `git tag --contains <commit> --sort=creatordate | head`.

In the latest deployed tag -> **Fixed**. Committed/tagged but that tag not deployed -> **Waiting for deploy**. When unsure whether a tag is deployed, ask.

## Investigation and tests

- Integration tests live under `tests/Campaign/`. Targeted unit tests mirror source layout.
- A reproducing test must be **green**, asserting the current buggy behavior, with a comment pinning it for `BGA #<id>` (flip the assertion when the fix lands) - see the skill's CONFIRMED note.

## Bug triage last checked

**2026-08-03 15:46 EDT** - only triage BGA reports created/updated after this time; bump this line to `date` at run start after each run.

## Tracked bugs

Internal record of triaged reports (root cause, fix, test) - never put this detail in a public bug comment.


- [ ] **Treetreader lets Alva walk OUT of Grimheim (pre-existing, found reviewing the #235064 fix).** NOT a reported bug yet. [FORUM.md:7036](FORUM.md#L7036), designer: "Treetreader may move into Grimheim but not out of it." Branch 1 `in(forest):move` is correctly void inside Grimheim (no terrain type), but branch 0 `move(forest)` is ungated: `hex_10_10` is forest and adjacent to Grimheim, and `getReachableHexes` seeds every Grimheim hex at distance 0 when the hero starts inside, so "Move into an adjacent forest area" walks her out of town. Predates the #235064 fix (the same move was offered under the other label before). FIX needs a negated gate, e.g. `notIn(Grimheim):spendUse:move(forest)` - the DSL has `Op_in` but no negation, so this means a new gate op or a `check(...)` bareword. No test yet. NOTE for Victoria: worth confirming this is really illegal before building a new DSL primitive for it.
- [ ] **`killed(<expr>)` quest predicates score the wrong monster in a multi-kill (found reviewing the #234247 fix).** NOT a reported bug yet, and NOT a regression - the reward half is fixed above, this is the quest-tracker half of the same root cause. [Op_killed](../../modules/php/Operations/Op_killed.php) and the `counter(countMonsterXp)` fallback still resolve the killed monster from `marker_attack`, which [Op_applyDamage](../../modules/php/Operations/Op_applyDamage.php) has already advanced. Repro: Bjorn with Nailed Together II killing two 1-XP goblins with a brute behind them advances the Trollbane tracker by 3 instead of 2. Same exposure for Quiver (`killed('rank>=3')`), Leather Purse, Helmet, and `CardEquip_Shield`. FIX: `queueTrigger` in `Op_applyDamage` passes no data - carry the dying monster id (e.g. `["killed" => $defenderId]`) and have `Op_killed` and `Op_counter` prefer it over the marker. No test yet; `Campaign_BjornQuestTest` around line 589 destructures an `$expectedTracker` it never asserts, which is why this stayed invisible - assert it as part of the fix.
- [ ] **Wrecking Ball should apply to ALL movement, not just the Move action (BGA #235051).** REOPENED (was Info-needed). The reporter is right per the designer: FORUM.md (BGG thread 3429134, designer reply #4) - asked whether Wrecking Ball works with Maneuver and Seek Shelter (the card says "move", not "move action"), the designer answered "Yes, it is always active :)". Current impl only wires the ram into the Move action: [Op_move::getPossibleMoves](../../modules/php/Operations/Op_move.php#L80) offers `card_ability_4_7`/`_4_8` as an extra target -> [Op_c_wrecking](../../modules/php/Operations/Op_c_wrecking.php). Movement from other sources (Seek Shelter, Maneuver, card-driven "Move N") never offers it. FIX (scope decision for Victoria): fire the Wrecking-Ball offering on any Boldur movement path, not just `Op_actionMove -> Op_move`. No test yet.
- [x] **RETRACTED: "Op_gainMana offers FULL cards and overflows past capacity" (found investigating BGA #235445).** NOT A BUG (2026-08-01) - the entry's premise was wrong: the `mana` material field is per-turn mana GENERATION ([Op_upgrade::generateTurnMana](../../modules/php/Operations/Op_upgrade.php#L127)), not storage capacity. Cards have no capacity and MUST accumulate past their gen value: Bracers and Sure Shot I generate 1/turn but their activations cost 3[MANA] (`3spendMana`) - saving up is the economy. "Bracers 1/1" was a misread; the client shows a plain crystal count, no x/y. A capacity-cap fix was attempted and broke 61 tests (every save-up flow: Focus, Home Sewn Cape runes, end-of-turn mana gen stacking, ...) - reverted, NO production change. `Op_gainManaBug235445Test` rewritten to pin the CORRECT behavior (prompts with 2 mana cards; card holding its gen value still offered; mana stacks beyond gen value). Designer ruling closes it: [FORUM.md:475](FORUM.md#L475) "Mana generation has no limit, you can have any amount of mana on a card... you can 'store up' several uses." #235445 itself stays Info-needed ("no prompt" not reproduced). Exhaustive sweep 2026-08-01: `Campaign_GainManaPromptBug235445Test` proves the full trigger path (forest move -> AlvaHeroI -> ?gainMana, 2 mana cards incl. the equip) prompts in BOTH solo and multiplayer (reporter's table is 4p, and #235314 taught us multiplayer-only bugs exist); only PHP delta between deployed tag and HEAD is the unrelated undo fix; harness snapshot (`debug_Op_gainMana235445`) shows the equip card gets active_slot + click listener same as ability cards; client has NO auto-submit path (every action_resolve is behind a click). Reporter is on Android Chrome Mobile, undid and got the same behavior twice, wanted the mana on Bracers. Leading theory: a tap got consumed as the prompt answer on mobile (layout shift / double-tap), or the status-bar prompt goes unnoticed on a phone. CLOSED 2026-08-03: reporter withdrew it - "I tried it again in and it worked this time. No Idea what went wrong when i created the report. You can Close this one and i will Open a new one and Stop the Game at the Point where it Happens." -> Works for me.
- [ ] **No way back to action selection after picking Move without stepping (from BGA #235314).** The reports #235314 and #235518 were CLOSED as Fixed on 2026-08-03 (Victoria's call: the undo barrier fix 82ca21c shipped in v260803-1041, reporters asked to file fresh if it recurs), but this sub-claim was never a snapshot problem and is not addressed by that fix: after clicking the Move icon with no step taken, the reporter had no way back to action selection. QUESTION for Victoria: should the move-selection state offer a cancel/back? The other half of #235314 - "nothing to undo" after a move - is taken to be the barrier-deletion bug fixed by 82ca21c; note the reporter pushed back 2026-08-02 on the earlier "you took the quest" explanation ("If you Look at the Quest it is the Same before and after movement. So no new Information was revealed"), so that explanation should not be reused.

- [x] **A "move" between two Grimheim hexes spends the move action and moves nothing (BGA #235653).** FIXED in working tree 2026-08-04, pending commit/deploy. [HexMap::getReachableHexes](../../modules/php/Common/HexMap.php#L502) seeded every Grimheim hex at distance 0 and removed only the hero's own start hex, so the other free Grimheim hexes were offered as legal move destinations. Clicking one: `Op_move::resolve` rewrote the target to the hero's home hex, then [HexMap::getPath](../../modules/php/Common/HexMap.php#L561) returned `[]` on its own guard ("Grimheim is a single area - moving between its hexes is a no-op"); zero step ops queued, the hero did not move, and the action marker was already parked. Matched the reporter verbatim: "The character did no movement but stayed in grimheim and I lost the movement action." Fix: `getReachableHexes` now unsets ALL Grimheim hexes when the hero starts inside, per [RULES.md:65](RULES.md#L65) ("one big area where any number of heroes may stand at the same time"). Moving OUT is untouched - the BFS still seeds Grimheim at distance 0 and only the destinations are dropped, so [RULES.md:67](RULES.md#L67) ("you may enter any area adjacent to it") still holds; `GameTest::testGetReachableHexes` now pins exits at distances 1, 2 and 3. Belt-and-braces: `Op_move::resolve` `systemAssert`s a non-empty path (`ERR:move:emptyPath:$from:$target`) instead of falling through with the action spent. Pinning tests flipped to the correct behaviour: `Campaign_UndoBug235518Test::testMoveInsideGrimheimIsNotOffered` (renamed from `testMoveInsideGrimheimSpendsTheActionWithoutMoving`), `Op_actionMoveTest::testReachableHexesFromGrimheim`, `GameTest::testGetReachableHexes`; `::testUndoAfterANoOpGrimheimMove` became `::testUndoAfterAMoveOutOfGrimheim` because the no-op move it drove is now impossible - it covers the report's other half (undo after a Move) on a real move. NOTE for Victoria: the two #235653 tests still live in the file named after #235518 (both reports were triaged in one pass); and `Op_moveStep::resolve` has the same empty-path shape but only re-prompts instead of losing an action, so it was left alone. Flip tracker status once deployed.

- [ ] **RETRACTED as a bug explanation, kept only as hygiene: the `player_id == 0` snapshot clear.** RETRACTED 2026-08-04 (Victoria). It was written up as the "best candidate" for the recurring "Nothing to undo" (#235518, #235653, #235314 remainder); that claim was WRONG and should not be reused. Two corrections: (1) clearing undo history when the turn passes to another player is INTENDED behavior, not damage - and the seat-switch is not even the `0` path, since [OpMachine.php:278](../../modules/php/OpCommon/OpMachine.php#L278) guards `$playerId > 0` and passes a real id, clearing and re-anchoring that one player's rows. (2) The reported symptom was already explained and fixed by 82ca21c (the restore deleted the turn-start barrier, so a second undo failed) - no second mechanism is needed to account for the reports. The residual point below is a code-hygiene nit, NOT a diagnosis, and no report should be attributed to it without a reproducing case. [DbMultiUndo::doSaveUndoSnapshot](../../modules/php/Db/DbMultiUndo.php#L70) runs `clearSnapshotsAfter`/`clearSnapshotsBefore` BEFORE its `player_id == 0 || player_id == 1` early return, and at `player_id == 0` both clears drop their `AND player_id = ...` filter ([DbMultiUndo.php:117](../../modules/php/Db/DbMultiUndo.php#L117)) - so with `barrier=1` the pair deletes `move_id >= X` and `move_id < X` for EVERY player, then returns without saving. The code comment concedes it: "basically with player_id = 0 it will clear all tables". Two callers pass `(int) getActivePlayerId()` unchecked during the monster turn, when there is no active player: [Game.php:479](../../modules/php/Game.php#L479) (`effect_doRollAttackDice`, which also runs for monster attacks) and [Op_rollMonsterDie.php:45](../../modules/php/Operations/Op_rollMonsterDie.php#L45). That `getActivePlayerId()` can return null/0 is already conceded by [Base::getActivePlayerColor](../../modules/php/Base.php#L262), which guards `$id === null || $id <= 0`; these two callers do not. Why this is NOT a diagnosis: both unguarded callers sit in the monster turn, i.e. between player turns, and the next turn re-anchors a barrier anyway - so even if the cast does hit 0, the clear is harmless in the normal flow. HARDENING (hygiene only): `systemAssert` on `player_id > 1` in `doSaveUndoSnapshot` instead of the silent `return` - a savepoint for player 0/1 should be loud, not a table wipe. Two weaker leads: `customUndoSavepoint` keeps only ONE pending `undoSavepointMeta` per request ([Game.php:857](../../modules/php/Game.php#L857)), so a seat-switch barrier plus a later reveal barrier in one request silently drops one; and [Base::doUndoSavePoint](../../modules/php/Base.php#L664) swallows a failed snapshot write with only an `error()`.

- [ ] **Smiterbiter steals Sweeping Strike's leftover damage, so the cleave is never offered (BGA #235866).** CONFIRMED 2026-08-04 - the ACTUAL root cause, found by Victoria loading the reporter's dump on the studio. The game log shows "Boldur stores 3 damage on Smiterbiter" on the move right after the kill: both `c_smiter` and `c_sweep` fire on `TMonsterKilled`, Smiterbiter consumes the excess first, and [Op_c_sweep::getPossibleMoves](../../modules/php/Operations/Op_c_sweep.php#L57) then bails with "No overkill damage" - which is why the cleave bullet is grayed. That is the reporter's screenshot explained; it has nothing to do with adjacency, the once-per-turn lock, or trigger merging (see RETIRED THEORIES below). RULES VIOLATION, designer ruling [FORUM.md:5232](FORUM.md#L5232) (thread "Smiterbiter + Sweeping Strike, what to do exactly with excess/remaining damage"): "You played correctly. 'Excess damage' is basically damage beyond what could be dealt, but if your sweeping strike goes against a second monster, the damage is dealt to that monster, so it isn't 'excess damage'. If you deal 5 damage and cut through 2 monsters with 2 health each, you would have 1 'excess damage'." So Sweeping Strike resolves FIRST and only what survives the cleave counts as excess for Smiterbiter - our ordering is backwards. SECOND DISCREPANCY, verify against the printed card before acting: the forum OP quotes Smiterbiter as "any excess damage MAY be stored here (max 3 stored)" and the designer did not correct that wording, but our [card_equip_material.csv:58](../../misc/card_equip_material.csv#L58) says "excess damage IS stored here" - if the real card says "may", our text turned an optional effect into a mandatory one, which is exactly how it silently wins the race. FIX (decision needed): resolve the cleave before Smiterbiter stores, and/or restore the "may" so the player chooses. No test yet for the real cause.

- [ ] **Latent: an answered prompt's continuation swallows a later trigger (found investigating BGA #235866, NOT its cause).** FIXED in working tree 2026-08-04, UNCOMMITTED, awaiting Victoria's review. [Card::promptUseCard](../../modules/php/Model/Card.php#L305) now merges a new trigger only into a pending `useCard` whose `excluded` data field is empty; an op carrying `excluded` is the tail of a prompt the player already answered (`Op_useCard::resolve` re-queues itself with `excluded` after every non-Manual play), so a later trigger gets its own fresh prompt instead of being offered under the stale event. Same-trigger dedup (two cards on one trigger produce ONE prompt) is unchanged. Tests: `testAnsweredPromptDoesNotSwallowALaterTrigger` is the guard and `testUnansweredPromptStillMergesASecondTrigger` pins the preserved merge, both in `tests/Operations/Op_useCardTest.php`; `testBoneBaneBowKillLeavesAnAnsweredPromptPending` in `tests/Campaign/Campaign_BjornEquipTest.php` pins the real state it keys on. The reported-combo pins live in `tests/Campaign/Campaign_BoldurSweepTest.php`. EVIDENCE IT IS NOT #235866: with the fix reverted the full suite gives 1311 tests / exactly 1 failure (that guard test alone), and Victoria confirmed by hand that the second Sweeping Strike prompt appears identically with and without the fix. Keep it as a latent-defect fix, not as a fix for any report. Piece two of the old mechanism is untouched: [Op_useCard::getPossibleMoves](../../modules/php/Operations/Op_useCard.php#L83) still `break`s on the first playable trigger; if a future card makes a mixed `on` list reachable, prefer the most recently merged trigger or offer the card once per matching trigger.

- [ ] **RETIRED THEORIES for BGA #235866 - do not re-investigate.** (1) "The second monster was not adjacent to Boldur" - WRONG, both brutes are adjacent (Victoria checked the screenshot; hex centres measured), and `Op_c_sweep` scans the hero's whole ring anyway. (2) "The once-per-turn ability lock blocks the second use" - WRONG, that lock lives only in `Op_spendUse` and Sweeping Strike has no `spendUse`, so the code complies with [RULES.md:420](RULES.md#L420). (3) "A merged trigger list mis-seeds the Op_or event" - real defect but unreachable in play, see the latent entry above. (4) "Rapid Strike / Eitri's Pick nest an attack inside a pending useCard" - WRONG, neither has an `on` field so both play under `Trigger::Manual`, which never re-queues. Also ruled out: stale `marker_attack` overkill/adjacency, `getAttackHex()` under Rapid Strike II nesting, and the clockwise direction (verified correct).

- [ ] **Sweeping Strike shows a permanently-unreachable grayed choice (UX follow-up from BGA #235866).** Separate from the bugs above - this is what made the report look "broken" to the player, and it is why the reporter never went looking for the second prompt. Note the second prompt only asks yes/no: `Op_c_sweep::getPossibleMoves` returns a SINGLE target chosen by clockwise order, and `getPrompt` already names it, so re-showing the whole card with one bullet grayed buys nothing. The `(A)/(B)` DSL is one `Op_or` instantiated per trigger, so at `TActionAttack` the `on(TMonsterKilled)` branch is voided by [Op_on](../../modules/php/Operations/Op_on.php#L44) and rendered grayed; the card re-prompts after the kill with the other bullet grayed instead. Both bullets DO fire from one activation, which is correct per [FORUM.md:4454](FORUM.md#L4454), designer: "That's right! The card applies in each attack action." FIX IDEA: have `Op_or::paramInfo` suppress branches whose `on(...)` gate cannot match the current event, so each prompt shows only its live bullet - same family as the Treetreader label fix 12df1da. Also cosmetic: [HexMap.php:104](../../modules/php/Common/HexMap.php#L104) comment labels are rotated one step off the vectors they annotate (says `NW (-1,0) -> NE (0,-1) -> ...`, but per `CLOCK_OFFSETS` those vectors are W, NW, NE, E, SE, SW); behavior is correct, comment is wrong.
- [ ] **Step-by-step move: reporter says it ends early on enemy-adjacency / quest-completion (BGA #234817).** Info-needed after investigation. Background agent PROVED server-side NOT A BUG for both sub-claims (green tests `Op_moveStepPrematureEndBug234817Test`, drives the real Op_moveStep loop end-to-end): (1) `HexMap::getReachableHexes` returns the free hexes around an adjacent monster (only excludes the occupied hex), so moveStep keeps prompting - it auto-ends only when the hero is genuinely boxed in (correct per RULES.md:61); (2) completing a TStep quest mid-move (Belt of Youth) does NOT void the moveStep re-queue - loop continues. The prior `HeroMoveAdjacentMonsterBug234817Test` (commit 758a6ad) is a FALSE GUARD (only calls getReachableHexes in isolation, never drives the loop / isTrivial auto-end). IF the reporter's symptom is real with free hexes remaining, it is a CLIENT (TypeScript) issue: the server enters an interactive prompt and never auto-ends while free hexes exist; the step prompt is rendered by the generic handler in `src/GameMachine.ts`. Client-side UNCONFIRMED - needs a live browser repro (chrome-devtools-mcp vs Studio). Asked reporter for a concrete case (open hex available but move still ended) to separate boxed-in (correct) from a client bug.

## Tracked bugs - posponeded
- [ ] **Sweeping Strike "1 cleave hit per attack" cap not enforced in code (follow-up from BGA #233927, pre-existing).** NOT a reported bug yet - flagged during the #233927 fix. The [Op_c_sweep](../../modules/php/Operations/Op_c_sweep.php) docblock claims at most 1 cleave hit per attack, but nothing enforces it; it relies on overkill running out. A cleave that leaves LEFTOVER overkill (e.g. target health 1, overkill 2) with a third live monster on the hero's ring could chain a second cleave. No test yet. Consider enforcing the cap explicitly (dedicated marker/flag) rather than depending on overkill arithmetic.
- [ ] **Optional step-by-step move mode for heroes without a step incentive (feature request, BGA #235315).** Heroes without a step incentive use one-shot `Op_move` (click the final destination, hero paths there and the action ends); players expecting hex-by-hex clicking are surprised. #235315 requests exactly this (with an "end movement" button) - reclassified to a `suggestion` for votes. Idea: offer an optional step-by-step move mode (game preference or per-move toggle). Design decision, not started. `Op_actionMove::hasStepIncentive` currently gates step mode; a preference would OR into it. (Distinct from the #234817 step-mode-ends-early BUG above, which is real.)

## Triage run log

Short log of each run: date, reports touched, outcome.

- **2026-08-04** - Follow-up day, no new reports triaged. #235653 Grimheim no-op move FIXED and committed (`bee76ba`) via the `game-bug-fix` skill; two PRE-EXISTING tests (`GameTest::testGetReachableHexes`, `Op_actionMoveTest::testReachableHexesFromGrimheim`) were found to be pinning the buggy behavior as correct and were flipped. #235866 went through three wrong causes before Victoria loaded the reporter's dump on the studio and read the game log: the leftover damage was being stored on Smiterbiter, so `Op_c_sweep` saw zero overkill and grayed the cleave. Designer ruling [FORUM.md:5232](FORUM.md#L5232) says the cleave resolves FIRST and only what survives it is "excess" - so this is a real rules bug, still open, now tracked with the correct cause; the retired theories are listed so nobody re-treads them. A one-line latent fix in `Card::promptUseCard` came out of the wrong-cause work - kept, uncommitted, proven NOT to be #235866 (full suite with it reverted: 1311 tests, 1 failure, its own guard test; Victoria also confirmed by hand the behavior is identical either way). PROCESS LESSON: #235866 was set to Confirmed on the strength of a green test that built the failing state by hand instead of reaching it through play - it pinned a real defect that no player can hit. A reproducing test only counts if it drives the actual player sequence; otherwise the report stays Open.

- **2026-07-23** - New Boldur reports from Vigilante8 (table 887519986): #233796 Dwarf Mail `${count}` -> Confirmed (green test `Campaign_DwarfMailLogTest`); #233793 Mining Equipment unusable -> Confirmed (green test `Campaign_MiningEquipmentTest`); #233794 health count not refreshing -> left Open (client-side lead, not server-testable). No deploy sweep needed (nothing Waiting for deploy). Marker bumped to 08:32.
- **2026-07-24** - Deploy sweep: tag v260724-0836 shipped fixes for #233794 (health refresh), #233793 (Mining Equipment), #233796 (Dwarf Mail `${count}`) -> all flipped to Fixed and removed from Tracked bugs above. New Open reports triaged and Confirmed via background repro tests (no production code edited): #233970 Sure Shot II overkill softlock (`Op_c_sureshotIITest::testStep2OneHpMonsterHasNoValidChoiceBug`); #233845 Riposte vs ranged/non-adjacent attacker (`Campaign_EmblaAbilityTest::testRiposteIWronglyOfferedAgainstNonAdjacentRangedAttacker`); #233927 Sweeping Strike re-offer loop (`Campaign_BoldurSweepTest::testSweepingStrikeReoffersAfterBothMonstersDead`). #233815 (Undo persists, references fixed #233685) left untouched - Victoria had already replied to the reporter. Marker bumped to 08:39.
- **2026-07-24 (2nd run)** - Deploy sweep: Victoria fixed the 3 Confirmed bugs and tag v260724-1039 shipped them -> #233970 (Sure Shot II overkill), #233845 (Riposte adjacency), #233927 (Sweeping Strike re-offer) all flipped Confirmed -> Fixed, removed from Tracked bugs. Kept a new follow-up entry: Sweeping Strike cleave-cap not enforced in code (out of scope of #233927). No new reports since marker; #233815 (Undo) still Open, only prior dev reply, left as-is. Marker bumped to 10:42.
- **2026-07-24 (3rd run)** - One new report: #234119 Home Sewn Tunic mend removes only 1 damage instead of all -> investigated (background agent), CONFIRMED with green test `Op_actionMendTunicBug234119Test::testMendOnTunicRemovesOnlyOneDamageInsteadOfAll`; the "remove all with one mend" ability is entirely unwired. Fix approach chosen by Victoria: hardcode the Tunic case in the Mend action. #233815 (Undo) now Info needed (Victoria's action), no new reporter activity. Marker bumped to 19:05.
- **2026-07-29** - Big 5-day backlog (~12 new Open reports). Deploy sweep: #234119 Tunic fix (commit 1671c3d) committed 07-25 but NOT in latest tag v260724-1039 -> flipped Confirmed -> Waiting for deploy. Studio error log (backend+frontend) essentially clean this week, so the block reports are game-logic softlocks, not crashes. Confirmed via background repro tests (green, no prod edits): #234247 Sweeping Strike multi-kill XP (+ #234242 Nailed Together -> Duplicate of #234247) `Campaign_MultiKillXpBugTest`; #234859 red-crystal removeDamage softlock `Op_removeDamageSoftlockTest`; #234580 Nailed Together II 0-piercing chain softlock `Op_c_nailedChainSoftlockTest`. Investigated but NOT reproduced -> Info needed with questions: #234817 hero move-stop-adjacent (code correctly continues past a monster; left `HeroMoveAdjacentMonsterBug234817Test` as a passing regression guard); #234927 Long Shot I range-2 (code correctly applies +2 at range 2 - it is an optional trigger the player must confirm; left `Campaign_LongShotRangeBugTest`). Undo cluster (framework-dependent, per Victoria not investigable from our code): kept #234789 "Undo not working" (5 votes, 3 corroborators) as canonical Open; marked #234929, #235015, #234251 Duplicate of #234789; #234441 (undo draws again - distinct symptom) left Open; #233815 stays Info needed. #234596 "No XP reward" -> Not a bug (reporter retracted: "miss clicked on reward"). #235016 green minis left as `suggestion`. Marker bumped to 17:06.
- **2026-07-31** - Deploy sweep: tag v260730-1944 shipped Victoria's fixes (b6ea271+12df1da Treetreader, f51de36 multi-kill XP, ca295a0 Nailed Together). Flipped Confirmed -> Fixed and removed from Tracked bugs: #235064 Treetreader, #234247 Sweeping Strike multi-kill XP, #234580 Nailed Together II. #234242 stays Duplicate (canonical #234247 now Fixed). Studio backend log clean (only metasite infra noise), no regressions on the new release. New reports (all from ysubmarine): #235315 "no single-stepping" -> reclassified to `suggestion` (feature request; also clarified Dvalin's Pick needs you to END adjacent to 3 mountains, TMove quest); #235437 gray minis -> `suggestion`, left. Reopened / re-triaged on reporter replies: #235051 Wrecking Ball -> Info-needed -> Open (reporter right per designer FORUM ruling "always active"; should apply to all movement, not just the Move action - tracked); #234817 Embla move -> Not-a-bug -> Open -> Info-needed (reporter clarified it's step mode ending early on enemy-adjacency / quest-completion; background agent proved server-side NOT a bug both ways, test `Op_moveStepPrematureEndBug234817Test`, prior guard was false - if real it's client-side TS, asked reporter for a concrete open-hex case). Concrete new investigations: #235445 Alva racial gainMana -> Info-needed (reported "no prompt" NOT reproduced - Bracers was full; but agent found a real latent bug: gainMana offers full cards + overflows, test `Op_gainManaBug235445Test`, tracked (RETRACTED 2026-08-01, see Tracked bugs)); #235314 undo-for-Move -> left Open, flagged for Victoria (undo owner; "nothing to undo after move" contradicts DESIGN.md:423). #234859 removeDamage softlock still Confirmed (no fix). Marker bumped to 18:54.
- **2026-08-03** - Deploy sweep: tag v260803-1041 shipped 04fb109 (empty-gain auto-skip) and 82ca21c (undo barrier). No reports were in Waiting-for-deploy. #234859 red-crystal removeDamage softlock Confirmed -> Fixed (git-verified), entry removed from Tracked bugs. Studio backend log clean for the week - no regressions from the deploy; the single frontend entry (GAMESERVER-JS-2C8, "Importing a module script failed", 5689 events / 712 users since 2025-04-09, culprit `terraformingmarsthedicegame`, no stack frame in our code) is BGA-platform noise, skipped. New reports: #235866 Boldur Sweeping Strike "cant select 2nd target" (solo, table 893187904) -> set Confirmed, but on a cause that turned out to be WRONG; see the 2026-08-04 log entry below for the real one. #235653 "Undo movement" -> the no-op Grimheim move is a real CONFIRMED bug (green test, tracked above); its "nothing to undo" half not reproduced. #235518 "Cannot undo after focus" -> hypothesis that the first player of turn 1 has no barrier DISPROVED (`Game.php:179` anchors one explicitly; green test `testFocusAsTheFirstActionOfTheGameCanBeUndone`). #235314 + #235518 -> both closed as Fixed on Victoria's call: the undo barrier fix 82ca21c is deployed and is believed to cover them, reporters told to open a fresh report if it recurs. Neither is proven fixed - the `player_id == 0` wipe hypothesis and the Move-cancel sub-claim stay tracked above, so a recurrence is expected to be possible. #235445 Alva racial -> reporter withdrew it -> Works for me. #235801 "Character model colour" already a `suggestion`, left alone. A `player_id == 0` snapshot-wipe lead was recorded and then RETRACTED on 2026-08-04 (Victoria: clearing history on player switch is intended, and 82ca21c already explains the reports) - see the tracked entry; do not re-raise it as a cause without a reproducing case. All six status changes posted and verified by reload: #234859 Fixed, #235445 Works for me, #235653 Confirmed, #235314 Fixed, #235518 Fixed, #235866 Confirmed. Marker bumped to 15:46.
- **2026-07-30** - Deploy sweep: tag v260729-2347 shipped the undo fix (2741ed6 + roll-barrier c9da577 + `Campaign_UndoTest`) and the Tunic fix (1671c3d). Flipped to Fixed and removed from Tracked bugs: #234119 Tunic (Waiting-for-deploy -> Fixed, git-verified); the undo cluster #234789 (canonical), #234441 (undo re-draws / "infinite draws with prepare"), #233815 (older undo, Info-needed -> Fixed) - Victoria confirmed the undo tests cover it, so flipped despite the "Nothing to undo" half not being locally testable (studio backend+frontend logs clean of undo/game errors on the current release; "Nothing to undo" is a UserException, not logged). Duplicates #234929/#235015/#234251/#234242 left as Duplicate (canonical now Fixed). Reporter replies handled: #234927 Long Shot I -> Not a bug (reporter confirmed clicking the optional ability works; answered their follow-up: pure +2 bonus, no downside, opt-in only for engine consistency); #234817 Embla move -> Not a bug (screenshots + game log confirmed one-shot `Op_move`: "Alva/Embla moves into Plains" one hex then action ends; she clicked a near hex, nothing lost - recorded the optional step-by-step-move idea Victoria floated). New reports: #235051 Boldur Wrecking Ball -> Info needed (feature works but is a separate selectable "Wrecking Ball" option, not automatic during a normal move; asked reporter whether the option appeared); #235064 Alva Treetreader multi-part (unlimited activation + wrong forest targets) -> Confirmed (background agent reproduced all 3 claims; green tests `Campaign_TreetreaderBug235064Test`; missing `spendUse:` + effect-bullet order reversed vs DSL branches - see tracked entry). Confirmed sweep unchanged (no fix yet): #234247, #234859, #234580. Marker bumped to 16:08.
