# Bug Triage - project config and state

Game-specific configuration and running state for the `game-bug-triage` skill.
The skill itself is game-independent; everything project-specific lives here.
Read this file at the start of every triage run.

## Game reference

- **BGA game id:** 2758 (Fate: Defenders of Grimheim)
- **Bug list:** https://boardgamearena.com/bugs?game=2758
- **Single report:** https://boardgamearena.com/bug?id=<REPORT_ID>

## Rules sources (for `rules`-type reports)

Check a reporter's claim against these, in order, before assuming the code is wrong:

1. [RULES.md](RULES.md) - the rulebook.
2. [FORUM.md](FORUM.md) - designer clarifications and rulings saved from the forum.

## Deploy model

Process: **creating a git tag = deployed.** To decide if a fix is live:

- Find the fix commit: `git log --oneline -i --grep='#<reportid>'` (commit messages reference `BGA #<id>`).
- Is it in the latest tag: `git merge-base --is-ancestor <commit> <latest-tag> && echo LIVE`.
- List tags containing it: `git tag --contains <commit> --sort=creatordate | head`.

In the latest deployed tag -> **Fixed**. Committed/tagged but that tag not deployed -> **Waiting for deploy**. When unsure whether a tag is deployed, ask.

## Investigation and tests

- Integration tests live under `tests/Campaign/`. Targeted unit tests mirror source layout.
- A reproducing test must be **green**, asserting the current buggy behavior, with a comment pinning it for `BGA #<id>` (flip the assertion when the fix lands) - see the skill's CONFIRMED note.

## Bug triage last checked

**2026-07-29 17:06 EDT** - only triage BGA reports created/updated after this time; bump this line to `date` at run start after each run.

## Tracked bugs

Internal record of triaged reports (root cause, fix, test) - never put this detail in a public bug comment.


- [x] **Home Sewn Tunic: one mend action removes only 1 damage, not all (BGA #234119).** FIXED, waiting for deploy. Card text ([Material.php:4746](../../modules/php/Material.php#L4746), `card_equip_1_23`): "Spend 1 mend action to remove all damage from this card." Wired by hardcoding the card id (Orebiter precedent, no DSL slot free - `r` is taken by `spendDurab:preventDamage`): [Op_removeDamage](../../modules/php/Operations/Op_removeDamage.php) strips all damage on a Tunic pick, capped by the remaining budget so the rest of Grimheim's 5 stays spendable; [Op_actionMend](../../modules/php/Operations/Op_actionMend.php) also offers the Tunic outside Grimheim (the ability has no location clause), where picking it replaces the normal 2 heal per RULES.md:213. Tests in `Op_actionMendTunicBug234119Test`.
- [ ] **Undo "Nothing to undo" + undo re-draws a card (BGA #234789 canonical; dups #234929/#235015/#234251; related #234441/#233815). FIX COMMITTED (2741ed6), not yet deployed; now covered by integration tests - `tests/Stubs/MultiUndoInMem.php` gives undo an in-memory store (real DbMultiUndo logic, stubbed storage) and `Campaign_UndoTest::testUndoAfterTheEventDrawDoesNotReplayTheDraw` fails on the pre-fix code with the reported symptom (undo puts `drawEvent` back on the machine and the card is drawn again). The "Nothing to undo" half depends on the real BGA move counter, which the harness only approximates, so keep #234789 Open pending the Studio check.** Applied fix (Game.php): added `protected array $undoSavepointMeta`, made `customUndoSavepoint` flag-only (sets meta + `undoSavepoint()`, no inline snapshot), added deferred `doCustomUndoSavePoint()` override doing `doSaveUndoSnapshot($meta,$player_id,false)` at end-of-request - verbatim match to wayfarers (incl. `isMultiActive()` early-return and solo `getFirstPlayer` guard). Base.php hook chain already byte-identical; DbMultiUndo/restore/action_undo untouched. `npm run lint:php` OK, full PHP suite green (1275 tests). Needs Studio check: undo no longer says "Nothing to undo" and no longer re-draws. Root cause (found by comparison to bga-wayfarers): Both fate and wayfarers hand-roll the SAME custom `DbMultiUndo` snapshot layer (both override `undoSavepoint()` to skip BGA-native save); `DbMultiUndo.php` is essentially byte-identical between the two and is NOT where the bug is. The divergence is WHEN the snapshot is captured: **wayfarers defers it to end-of-request; fate takes it inline mid-action.** Correct pattern (wayfarers, `../bga-wayfarers/modules/php/Game.php`): `customUndoSavepoint` only sets `$undoSavepointMeta` + calls `undoSavepoint()` (flag only, no DB write); the real snapshot happens later in a `doCustomUndoSavePoint()` override invoked from the `sendNotifications` -> `doUndoSavePoint()` hook, AFTER the action settled / notifications flushed / op destroyed (their `phptests/GameTest.php` asserts `customUndoSavepoint` saves 0 snapshots immediately). Fate (buggy): [Game.php:851-859](../../modules/php/Game.php#L851) calls `dbMultiUndo->doSaveUndoSnapshot(...,true)` INLINE, and fate never overrides `doCustomUndoSavePoint`. Consequences: (1) **re-draw** (#234441) - inline snapshot freezes the still-pending draw op (rank>0) on the saved `machine` (e.g. [Hero::drawEventCard](../../modules/php/Model/Hero.php#L137)), so restore replays it and draws a NEW card; wayfarers already fixed/tested exactly this. (2) **"Nothing to undo"** - `doSaveUndoSnapshot` computes `move_id = getNextMoveId()-1` assuming it runs AFTER notify ([DbMultiUndo.php:70](../../modules/php/Db/DbMultiUndo.php#L70)); fate calls it BEFORE notifications flush, so the stored move id misaligns with the framework move counter and undo selects nothing -> `errorCannotUndo()`. FIX (port wayfarers structure, LOW invasiveness, ~2 methods + 1 property in Game.php; DbMultiUndo/restore/`action_undo` untouched, no schema change): make `customUndoSavepoint` only set `$undoSavepointMeta` + raise the flag (drop the inline `doSaveUndoSnapshot`, add the solo getFirstPlayer guard wayfarers has); add an `$undoSavepointMeta` property and a `doCustomUndoSavePoint()` override that calls `doSaveUndoSnapshot($meta,$player_id,false)`; move the undo-button notify off the inline path. Then the `Op_savepoint` manual `destroy()`-first ordering ([Op_savepoint.php](../../modules/php/Operations/Op_savepoint.php)) becomes an unnecessary workaround. NOT reproduced (read-only compare; symptoms reasoned from code + wayfarers' own regression tests).
- [ ] **Multi-kill abilities award XP/gold for only 1 monster (BGA #234247 Sweeping Strike; #234242 Nailed Together = duplicate).** CONFIRMED (green test `Campaign_MultiKillXpBugTest::testSweepMultiKillAwardsXpForOnlyOneMonster`). [Game::countMonsterXp](../../modules/php/Game.php#L591) ignores its `$context` (the monster being finalized) whenever an attack is in progress and instead reads the monster on the shared `marker_attack` hex. In a multi-kill, the cleave's `Op_applyDamage` advances `marker_attack` to the next monster before the first kill's `finishKill`/`finalizeDamage` runs, so the earlier kills read a stale/removed hex and score 0; only the last kill's reward lands. FIX: prefer explicit `$context` when it is a real monster id, fall back to attack-hex only when null (`$monsterId = $context; if null -> hexMap->getCharacterOnHex(getAttackHex())`). Re-run Boldur/Bjorn quest tests (Trollbane `counter(countMonsterXp)` is the only other caller; unchanged on single kills). Flip the test assertion (1 -> 2) once fixed.
- [ ] **Heal / removeDamage softlocks when there is no damage to remove (BGA #234859 red crystals).** CONFIRMED (green test `Op_removeDamageSoftlockTest::testGain3RemoveDamageWithNoDamageSoftlocks`). Gaining red crystals queues `NremoveDamage`; when nothing on the board has damage, [Op_removeDamage::getPossibleMoves](../../modules/php/Operations/Op_removeDamage.php) marks every candidate `ERR_NOT_APPLICABLE` -> `noValidTargets()` true, but [Op_removeDamage::canSkip](../../modules/php/Operations/Op_removeDamage.php#L240) returns false, so `canResolveAutomatically()` is false and the top-level dispatch drops into PlayerTurn with no clickable target and no Skip = softlock. (Top-level dispatch never consults `isVoid()`, which is why only the direct-reward path locks; nested-in-seq is fine.) FIX: `Op_removeDamage::canSkip()` should return true when `noValidTargets()` (auto-skip empty). Same base op is shared by heal/repairCard/`all`-mode, so the one fix covers those latent cases. Flip the `canSkip`/`canResolveAutomatically` assertions once fixed.
- [ ] **Nailed Together II chain re-offers 0-piercing after an exact kill -> softlock (BGA #234580).** CONFIRMED (green test `Op_c_nailedChainSoftlockTest::testChainReoffersZeroOverkillPierceAfterExactKill`). [Op_c_nailed.php:94](../../modules/php/Operations/Op_c_nailed.php#L94) re-queues `c_nailed(chain)` whenever `$willKill` (totalDamage >= health), so an EXACT kill (0 overkill) re-queues a chain step that is void (`getOverkill() <= 0` -> `ERR_NOT_APPLICABLE`) but not skippable: the plain `queue()` passes no `l_skip`, unlike the card-driven op which gets `l_skip=true` from [Card::createOperationForCardEffect](../../modules/php/Model/Card.php#L296). So the void op can't auto-resolve -> "Choose a monster to deal 0 piercing damage to" / "No overkill damage" softlock. FIX: `$this->queue("c_nailed(chain)", null, ["l_skip" => true]);` (also covers the no-monster-behind void case). Flip once fixed.
- [ ] **Sweeping Strike "1 cleave hit per attack" cap not enforced in code (follow-up from BGA #233927, pre-existing).** NOT a reported bug yet - flagged during the #233927 fix. The [Op_c_sweep](../../modules/php/Operations/Op_c_sweep.php) docblock claims at most 1 cleave hit per attack, but nothing enforces it; it relies on overkill running out. A cleave that leaves LEFTOVER overkill (e.g. target health 1, overkill 2) with a third live monster on the hero's ring could chain a second cleave. No test yet. Consider enforcing the cap explicitly (dedicated marker/flag) rather than depending on overkill arithmetic.

## Triage run log

Short log of each run: date, reports touched, outcome.

- **2026-07-23** - New Boldur reports from Vigilante8 (table 887519986): #233796 Dwarf Mail `${count}` -> Confirmed (green test `Campaign_DwarfMailLogTest`); #233793 Mining Equipment unusable -> Confirmed (green test `Campaign_MiningEquipmentTest`); #233794 health count not refreshing -> left Open (client-side lead, not server-testable). No deploy sweep needed (nothing Waiting for deploy). Marker bumped to 08:32.
- **2026-07-24** - Deploy sweep: tag v260724-0836 shipped fixes for #233794 (health refresh), #233793 (Mining Equipment), #233796 (Dwarf Mail `${count}`) -> all flipped to Fixed and removed from Tracked bugs above. New Open reports triaged and Confirmed via background repro tests (no production code edited): #233970 Sure Shot II overkill softlock (`Op_c_sureshotIITest::testStep2OneHpMonsterHasNoValidChoiceBug`); #233845 Riposte vs ranged/non-adjacent attacker (`Campaign_EmblaAbilityTest::testRiposteIWronglyOfferedAgainstNonAdjacentRangedAttacker`); #233927 Sweeping Strike re-offer loop (`Campaign_BoldurSweepTest::testSweepingStrikeReoffersAfterBothMonstersDead`). #233815 (Undo persists, references fixed #233685) left untouched - Victoria had already replied to the reporter. Marker bumped to 08:39.
- **2026-07-24 (2nd run)** - Deploy sweep: Victoria fixed the 3 Confirmed bugs and tag v260724-1039 shipped them -> #233970 (Sure Shot II overkill), #233845 (Riposte adjacency), #233927 (Sweeping Strike re-offer) all flipped Confirmed -> Fixed, removed from Tracked bugs. Kept a new follow-up entry: Sweeping Strike cleave-cap not enforced in code (out of scope of #233927). No new reports since marker; #233815 (Undo) still Open, only prior dev reply, left as-is. Marker bumped to 10:42.
- **2026-07-24 (3rd run)** - One new report: #234119 Home Sewn Tunic mend removes only 1 damage instead of all -> investigated (background agent), CONFIRMED with green test `Op_actionMendTunicBug234119Test::testMendOnTunicRemovesOnlyOneDamageInsteadOfAll`; the "remove all with one mend" ability is entirely unwired. Fix approach chosen by Victoria: hardcode the Tunic case in the Mend action. #233815 (Undo) now Info needed (Victoria's action), no new reporter activity. Marker bumped to 19:05.
- **2026-07-29** - Big 5-day backlog (~12 new Open reports). Deploy sweep: #234119 Tunic fix (commit 1671c3d) committed 07-25 but NOT in latest tag v260724-1039 -> flipped Confirmed -> Waiting for deploy. Studio error log (backend+frontend) essentially clean this week, so the block reports are game-logic softlocks, not crashes. Confirmed via background repro tests (green, no prod edits): #234247 Sweeping Strike multi-kill XP (+ #234242 Nailed Together -> Duplicate of #234247) `Campaign_MultiKillXpBugTest`; #234859 red-crystal removeDamage softlock `Op_removeDamageSoftlockTest`; #234580 Nailed Together II 0-piercing chain softlock `Op_c_nailedChainSoftlockTest`. Investigated but NOT reproduced -> Info needed with questions: #234817 hero move-stop-adjacent (code correctly continues past a monster; left `HeroMoveAdjacentMonsterBug234817Test` as a passing regression guard); #234927 Long Shot I range-2 (code correctly applies +2 at range 2 - it is an optional trigger the player must confirm; left `Campaign_LongShotRangeBugTest`). Undo cluster (framework-dependent, per Victoria not investigable from our code): kept #234789 "Undo not working" (5 votes, 3 corroborators) as canonical Open; marked #234929, #235015, #234251 Duplicate of #234789; #234441 (undo draws again - distinct symptom) left Open; #233815 stays Info needed. #234596 "No XP reward" -> Not a bug (reporter retracted: "miss clicked on reward"). #235016 green minis left as `suggestion`. Marker bumped to 17:06.
