# Bug Triage - project config and state

Game-specific configuration and running state for the `game-bug-triage` skill.
The skill itself is game-independent; everything project-specific lives here.
Read this file at the start of every triage run.

## Game reference

- **BGA game id:** 2758 (Fate: Defenders of Grimheim)
- **Bug list:** https://boardgamearena.com/bugs?game=2758
- **Single report:** https://boardgamearena.com/bug?id=<REPORT_ID>
- **Studio error log:** https://studio.boardgamearena.com/studioissues?game=fate - studio short name is `fate`, not the numeric id above. Add `&tableId=<TABLE_ID>` for one table, `&id=<ERROR_ID>` for one error, `&projects=frontend|backend` to filter. Verified working 2026-08-17.
- **Designer:** BGA user **Lord Aethan** (player id 91033064) is the game designer. His reports are design rulings, not player claims - never post comments or change status on his reports without Victoria's explicit go-ahead.

## Rules sources (for `rules`-type reports)

Check a reporter's claim against these, in order, before assuming the code is wrong:

1. [RULES.md](RULES.md) - the rulebook - you must read this no matter what you doing.
2. [FORUM.md](FORUM.md) - designer clarifications and rulings saved from the forum.

## Deploy model

Process: **creating a git tag = deployed.** To decide if a fix is live:

- Find the fix commit: `git log --oneline -i --grep='#<reportid>'` (commit messages reference `BGA #<id>`).
- Is it in the latest tag: `git merge-base --is-ancestor <commit> <latest-tag> && echo LIVE`.
- List tags containing it: `git tag --contains <commit> --sort=creatordate | head`.

In the latest deployed tag -> **Fixed**. Committed/tagged but that tag not deployed -> **Waiting for deploy**. When unsure whether a tag is deployed, ask.

## Investigation and tests

- Integration tests live under `tests/Campaign/`. Targeted unit tests mirror source layout.
- A reproducing test must be **green**, asserting the current buggy behavior, with a comment pinning it for `BGA #<id>` (flip the assertion when the fix lands) - see the skill's CONFIRMED note.

## Bug triage last checked

**2026-08-17 08:22 EDT** - only triage BGA reports created/updated after this time; bump this line to `date` at run start after each run.

## Tracked bugs

Internal record of triaged reports (root cause, fix, test) - never put this detail in a public bug comment.

- [x] **Standing on a terrain does not count as adjacent to it (BGA #238473, DESIGNER report).** FIXED: `countAdjMountains` ([Game.php:544](../../modules/php/Game.php#L544)) now also counts the hero's own hex when its terrain is mountain, per [RULES.md:412](RULES.md#L412) adjacency and the designer's ruling on the report; fixes Dvalin's Pick (card_equip_4_20) and Mining Equipment (card_equip_4_17) in Troll Caves. Test: `Campaign_TerrainAdjacencyTest` (own-hex count, neighbor-only guard, Grimheim guard, Dvalin's Pick end-to-end). Note: Grimheim hexes DO have terrain (plains) contrary to the earlier root-cause note - still excluded since plains != mountain. Follow-up (Victoria ruled the adjacency ruling covers it, now DONE): [Op_c_orebiter](../../modules/php/Operations/Op_c_orebiter.php) targeted only NEIGHBOR mountains - it now offers the hero's own mountain hex too, and the attack pipeline resolves the gold vein (not the hero sharing the hex) as the defender via the `getCharacterOnHex` exclude-attacker parameter. Tests: `Campaign_TerrainAdjacencyTest::testOrebiterMinesTheMountainHeroStandsOn` (end-to-end: gold paid, hero undamaged), `Op_c_orebiterTest::testOwnMountainHexIsValidTarget`. NO tracker action taken (designer report - Victoria to decide the reply).
- [ ] **Wrecking Ball ram not usable / step-by-step movement wanted as the norm (BGA #238475, DESIGNER report).** Reported by Lord Aethan (same solo table 900570224, dump move 71, no server errors). Two asks: (1) he could not move into occupied areas at all; (2) a move action should track steps and stay open until "end movement" or another action - explicitly generalized in his comment to collecting markers mid-move. On deployed v260816-1201 the ram IS implemented but only as a click on the Wrecking Ball CARD in the move target list ([Op_move](../../modules/php/Operations/Op_move.php) live version, Orebiter pattern) - occupied hexes are never directly clickable, and the newly shipped map tinting (a9d40ad) paints them as non-targets, so the most likely reading is a discoverability failure, not a void offer; UNCONFIRMED whether something also voided the card offer at his board state (dump not loaded). Undeployed HEAD already reworks exactly this: 6e4a90a offers the ram inside step mode instead of vetoing step mode, and 6f1fc02 adds pref 102 "Confirm end of movement" (opt-in step mode for everyone) - together they cover most of ask (2), except he wants it default-on, not opt-in. Related: #235051 (Wrecking Ball on all movement - his report reinforces it), #235315 (the step-mode suggestion, implemented). NO tracker action taken (designer report - Victoria to decide scope and reply). IMPLEMENTED 2026-08-18 (undeployed): occupied hexes within the move budget are now directly clickable move targets (distant ones auto-route) - Wrecking Ball is itself a step incentive so Boldur always moves in [Op_move](../../modules/php/Operations/Op_move.php) step mode; the card-button offer is gone and [Op_c_wrecking](../../modules/php/Operations/Op_c_wrecking.php) is reduced to the push phase. When a hex is both an attack target and a Wrecking Ball move target, Op_turn queues an `actionMove/actionAttack` or-expression (server-side chooser). Covers ask (1) fully; ask (2) was already covered by pref 102 default-on (f65a6a1).

- [ ] **countAdj* helpers leak adjacency across the Grimheim border (pre-existing, found reviewing the #238473 fix).** [RULES.md:65](RULES.md#L65): heroes inside Grimheim are "not adjacent to the surrounding areas" and "cannot interact with characters or terrain outside of it". TERRAIN half FIXED per Victoria's ruling ("You are not considered adjacent to the mountain when in Grimheim, because you cannot interact"): `countAdjMountains` now returns 0 when the hero's hex is in Grimheim, and `Op_c_orebiter` offers no mountain from inside town (its own hex walk is wrapped in the same guard, so the border mountain hex_9_11 next to hex_9_10 is out of reach). Tests: `Campaign_TerrainAdjacencyTest` (counter zero on hex_9_10, Mining Equipment void, Orebiter absent from the attack list), `Op_c_orebiterTest::testGrimheimBorderMountainIsNotTarget`. STILL OPEN for Victoria: whether the isolation also zeroes `countAdjMonsters`/`countAdjLegends` ([Game.php:529](../../modules/php/Game.php#L529)) - monsters ringing town feed Eitri's Pick and Home Sewn Cape gates today; that monster-adjacency half is untouched pending her call.
- [ ] **Treetreader lets Alva walk OUT of Grimheim (pre-existing, found reviewing the #235064 fix).** NOT a reported bug yet. [FORUM.md:7036](FORUM.md#L7036), designer: "Treetreader may move into Grimheim but not out of it." Branch 1 `in(forest):move` is correctly void inside Grimheim (no terrain type), but branch 0 `move(forest)` is ungated: `hex_10_10` is forest and adjacent to Grimheim, and `getReachableHexes` seeds every Grimheim hex at distance 0 when the hero starts inside, so "Move into an adjacent forest area" walks her out of town. Predates the #235064 fix (the same move was offered under the other label before). FIX needs a negated gate, e.g. `notIn(Grimheim):spendUse:move(forest)` - the DSL has `Op_in` but no negation, so this means a new gate op or a `check(...)` bareword. No test yet. NOTE for Victoria: worth confirming this is really illegal before building a new DSL primitive for it.
- [ ] **`killed(<expr>)` quest predicates score the wrong monster in a multi-kill (found reviewing the #234247 fix).** NOT a reported bug yet, and NOT a regression - the reward half is fixed above, this is the quest-tracker half of the same root cause. [Op_killed](../../modules/php/Operations/Op_killed.php) and the `counter(countMonsterXp)` fallback still resolve the killed monster from `marker_attack`, which [Op_applyDamage](../../modules/php/Operations/Op_applyDamage.php) has already advanced. Repro: Bjorn with Nailed Together II killing two 1-XP goblins with a brute behind them advances the Trollbane tracker by 3 instead of 2. Same exposure for Quiver (`killed('rank>=3')`), Leather Purse, Helmet, and `CardEquip_Shield`. FIX: `queueTrigger` in `Op_applyDamage` passes no data - carry the dying monster id (e.g. `["killed" => $defenderId]`) and have `Op_killed` and `Op_counter` prefer it over the marker. No test yet; `Campaign_BjornQuestTest` around line 589 destructures an `$expectedTracker` it never asserts, which is why this stayed invisible - assert it as part of the fix.
- [X] **Wrecking Ball should apply to ALL movement, not just the Move action (BGA #235051).** REOPENED (was Info-needed). The reporter is right per the designer: FORUM.md (BGG thread 3429134, designer reply #4) - asked whether Wrecking Ball works with Maneuver and Seek Shelter (the card says "move", not "move action"), the designer answered "Yes, it is always active :)". Current impl only wires the ram into the Move action: [Op_move::getPossibleMoves](../../modules/php/Operations/Op_move.php#L80) offers `card_ability_4_7`/`_4_8` as an extra target -> [Op_c_wrecking](../../modules/php/Operations/Op_c_wrecking.php). Movement from other sources (Seek Shelter, Maneuver, card-driven "Move N") never offers it. FIX (scope decision for Victoria): fire the Wrecking-Ball offering on any Boldur movement path, not just `Op_actionMove -> Op_move`. No test yet. IMPLEMENTED 2026-08-18 (undeployed) for unfiltered movement: [Op_move](../../modules/php/Operations/Op_move.php) delegates to moveStep (which owns the occupied-hex targets) whenever the card is on the tableau, so Maneuver / plain "Nmove" card effects get the Wrecking Ball move. Filtered moves (Seek Shelter `move(locationOnly)`, Treetreader `move(forest)`) still lack it - destination-filter-in-step-mode is the open question in [PLAN_WRECKING_MOVE.md](PLAN_WRECKING_MOVE.md).

- [ ] **No way back to action selection after picking Move without stepping (from BGA #235314).** The reports #235314 and #235518 were CLOSED as Fixed on 2026-08-03 (Victoria's call: the undo barrier fix 82ca21c shipped in v260803-1041, reporters asked to file fresh if it recurs), but this sub-claim was never a snapshot problem and is not addressed by that fix: after clicking the Move icon with no step taken, the reporter had no way back to action selection. QUESTION for Victoria: should the move-selection state offer a cancel/back? The other half of #235314 - "nothing to undo" after a move - is taken to be the barrier-deletion bug fixed by 82ca21c; note the reporter pushed back 2026-08-02 on the earlier "you took the quest" explanation ("If you Look at the Quest it is the Same before and after movement. So no new Information was revealed"), so that explanation should not be reused.
- [ ] **Draugr armor only reduces attack-dice damage, not every source (BGA #236177).** Reporter is RIGHT per the rulebook: [RULES.md:350](RULES.md#L350) "**Draugr Additional Effect:** Draugr have armor (prevent 1 damage each time it is dealt damage)", plus two designer confirmations - [FORUM.md:7663](FORUM.md#L7663) (asked whether two 1-damage event cards in one turn therefore deal nothing to a draugr: "Correct.") and [FORUM.md:1461](FORUM.md#L1461) ("That's right, Hail of Arrows is useless against draugr"). Screenshots show Courage/kick damage and Riposte damage landing in full while attack damage is correctly reduced. Root cause: `applyArmor` had exactly ONE caller, `Op_resolveHits::queueDamage`, on the dice-resolution path, so any damage not coming from battle dice bypassed armor entirely. Same exposure for Boldur's own armor against non-attack monster damage. FIXED (not yet deployed): the reduction moved into [Op_dealDamage::getEffectiveDamage](../../modules/php/Operations/Op_dealDamage.php), which every preventable damage source funnels through, carrying the `canIgnoreArmor` consult (Eagle Eye II) with it. Three things the move needed: (1) [Op_killMonster](../../modules/php/Operations/Op_killMonster.php) extends `Op_dealDamage`, so it opts out via `isArmorApplicable()` - an outright kill is not damage and armor must not leave the draugr on 1 health; (2) [Op_preventDamage](../../modules/php/Operations/Op_preventDamage.php) reads `getEffectiveDamage()` rather than the raw count, because armor resolves first ([FORUM.md:6519](FORUM.md#L6519)) - otherwise Boldur's once-per-turn Dwarf Mail gets offered against, and burned on, a hit his armor already ate; (3) prevention still subtracts from the raw count so armor is applied exactly once. Damage that reaches `Op_applyDamage` directly stays unarmored by design - that path is unpreventable damage (Berserk's `spendHealth`, Seer of Odin II, Smiterbiter excess), and unpreventable means armor does not apply either. Tests: `Op_dealDamageTest` (6 armor cases), `Op_preventDamageTest` (3 armor-vs-prevention cases), `Op_killMonsterTest::testArmorDoesNotBluntAnOutrightKill`, `Campaign_BoldurEventTest::testKickDamageIsAbsorbedByDraugrArmor` and `testDodgeNotOfferedWhenArmorAbsorbsTheWholeHit`. Solo table 893989385 (dump is state 99, end of game, so not replayable at the moment of the bug). Player-facing comment already posted confirming the rule; flip to Fixed on the next deploy sweep.
- [ ] **Undo refused right after an Upgrade action (BGA #236178).** Reporter upgraded Sweeping Strike I -> II, got "nothing to undo", and argues no new information was revealed; they accept that LEARNING a new skill should not be undoable and separately ask for a warning before it, like the draw warning. HYPOTHESIS ONLY, no test written: nothing in the upgrade path anchors a barrier - the only `customUndoSavepoint` callers are turn start ([Game.php:179](../../modules/php/Game.php#L179)), card draw ([Hero.php:137](../../modules/php/Model/Hero.php#L137)), attack and monster dice ([Game.php:479](../../modules/php/Game.php#L479), [Op_rollMonsterDie.php:45](../../modules/php/Operations/Op_rollMonsterDie.php#L45)) and the seat switch ([OpMachine.php:280](../../modules/php/OpCommon/OpMachine.php#L280)); `Op_savepoint` exists as an op but no material CSV uses it. [DESIGN.md:423](DESIGN.md#L423) lists the barriers as "turn start, and immediately after information is revealed (event draw, attack dice, monster die)" - an upgrade is not among them, so undo should work unless something else in that flow anchors one. Solo table 893187904, dump at move 68. QUESTION for Victoria (undo owner): is an upgrade meant to be a barrier because the level II side becomes readable? If yes this is a UX ask (warn before upgrading); if no it is a real undo bug.
- [ ] **No way to back out of an event card's targeting prompt (BGA #236519).** Same shape as the #235314 Move-without-stepping sub-claim tracked above, now reported by a second player: they selected an event card that moves 2 spaces, saw only two highlighted hexes, decided against it, and had no cancel - Undo answered "nothing to undo". HYPOTHESIS, no test: the dump is move 3, i.e. turn 1, and drawing the event anchors a barrier ([Hero.php:137](../../modules/php/Model/Hero.php#L137), label "draw"); selecting a card to play is not itself an undoable decision, so there may be genuinely nothing behind the cursor to rewind. If that holds, the fix is a cancel/back affordance out of a targeting state rather than an undo change - the same decision already owed on the Move entry above. Solo table 894412395. Left OPEN, no comment posted.

- [ ] **RETRACTED as a bug explanation, kept only as hygiene: the `player_id == 0` snapshot clear.** RETRACTED 2026-08-04 (Victoria). It was written up as the "best candidate" for the recurring "Nothing to undo" (#235518, #235653, #235314 remainder); that claim was WRONG and should not be reused. Two corrections: (1) clearing undo history when the turn passes to another player is INTENDED behavior, not damage - and the seat-switch is not even the `0` path, since [OpMachine.php:278](../../modules/php/OpCommon/OpMachine.php#L278) guards `$playerId > 0` and passes a real id, clearing and re-anchoring that one player's rows. (2) The reported symptom was already explained and fixed by 82ca21c (the restore deleted the turn-start barrier, so a second undo failed) - no second mechanism is needed to account for the reports. The residual point below is a code-hygiene nit, NOT a diagnosis, and no report should be attributed to it without a reproducing case. [DbMultiUndo::doSaveUndoSnapshot](../../modules/php/Db/DbMultiUndo.php#L70) runs `clearSnapshotsAfter`/`clearSnapshotsBefore` BEFORE its `player_id == 0 || player_id == 1` early return, and at `player_id == 0` both clears drop their `AND player_id = ...` filter ([DbMultiUndo.php:117](../../modules/php/Db/DbMultiUndo.php#L117)) - so with `barrier=1` the pair deletes `move_id >= X` and `move_id < X` for EVERY player, then returns without saving. The code comment concedes it: "basically with player_id = 0 it will clear all tables". Two callers pass `(int) getActivePlayerId()` unchecked during the monster turn, when there is no active player: [Game.php:479](../../modules/php/Game.php#L479) (`effect_doRollAttackDice`, which also runs for monster attacks) and [Op_rollMonsterDie.php:45](../../modules/php/Operations/Op_rollMonsterDie.php#L45). That `getActivePlayerId()` can return null/0 is already conceded by [Base::getActivePlayerColor](../../modules/php/Base.php#L262), which guards `$id === null || $id <= 0`; these two callers do not. Why this is NOT a diagnosis: both unguarded callers sit in the monster turn, i.e. between player turns, and the next turn re-anchors a barrier anyway - so even if the cast does hit 0, the clear is harmless in the normal flow. HARDENING (hygiene only): `systemAssert` on `player_id > 1` in `doSaveUndoSnapshot` instead of the silent `return` - a savepoint for player 0/1 should be loud, not a table wipe. Two weaker leads: `customUndoSavepoint` keeps only ONE pending `undoSavepointMeta` per request ([Game.php:857](../../modules/php/Game.php#L857)), so a seat-switch barrier plus a later reveal barrier in one request silently drops one; and [Base::doUndoSavePoint](../../modules/php/Base.php#L664) swallows a failed snapshot write with only an `error()`.


- [ ] **Sweeping Strike re-kills the same monsters and pays gold each time (BGA #236791).** HYPOTHESIS ONLY, no test - do not act on it as fact. Reporter (4p table 893045130, Boldur turn 7, move 308, filed 2026-08-06 21:10, i.e. ~1h after v260806-2001 shipped a3041ad): "His sweep attack appears to kill the same two enemies 3 times each and rewards gold for each kill." Suspected re-entry: the kill trigger runs BEFORE `Op_finishKill` clears the corpse (stated in a3041ad's own message), so [Op_c_sweep::getSweepHex](../../modules/php/Operations/Op_c_sweep.php#L47) walking clockwise via `getCharacterOnHex($hex, "monster")` can return a monster that is dead but still on its hex; `marker_attack` has moved to the new victim, so a second sweep starts from there and can land back on the first corpse, where `Op_applyDamage` sees damage >= health, reports `killed` again and queues another `finishKill` -> XP/gold twice. The only brake is the new `if ($this->op->getDataField("spendsExcess")) return;` guard in [CardAbility_SweepingStrikeI::onMonsterKilled](../../modules/php/Cards/CardAbility_SweepingStrikeI.php) - if that field does not survive `queueTrigger -> queue("trigger(TMonsterKilled)", $owner, $data)` the guard reads null and the 2-enemy cap silently evaporates. Note this is the same SHAPE as #233927 (fixed 2026-07-24), so treat a "we already fixed that" reflex with suspicion.

  **UPDATE 2026-08-12 - the hypothesis above is DEAD, do not re-tread it.** Investigation agent drove the exact state it needs through real play (solo Boldur, Sweeping Strike I, goblin W as attack target and goblin NW pre-damaged, 4 damage on a 2-health goblin so the sweep kills with 1 damage STILL left over and both corpses still on their hexes): each goblin dies once and scores 1 XP once, machine settles back on `turn`. Green regression guard `Campaign_BoldurSweepTest::testSweepMultiKillScoresEachMonsterOnce`. And the premise is disproved directly: **`spendsExcess` DOES survive the queue** - `testTriggerDataFieldSurvivesTheQueue` re-reads the op from its db row and gets `true`, because `OpMachine::instantiateOperation` applies `withData` after `withParams` and `Operation::withData` merges instead of replacing. So the 2-enemy cap really exists. Still UNCONFIRMED, not disproved: the report is a 4-PLAYER table (only solo was driven; `Op_trigger` walks just the owner's tableau, so no seam is visible but it was not tested), and Sweeping Strike **II** combined with a second attack action (Rapid Strike II) could put two kill-pairs in one turn's log. One cosmetic finding that could explain a miscount outright: `finishKill` for the SWEEP victim runs BEFORE `finishKill` for the primary victim, so the game log prints the two kills in reverse order. ASK THE REPORTER: the exact log lines, whether their XP/gold total actually rose by more than the monsters were worth (versus the log merely reading repetitively), and whether Smiterbiter was in their tableau - its pending excess is parked as YELLOW crystals on the card, which look like gold.
- [ ] **Attack greyed out with "no valid targets" while monsters are adjacent (BGA #236916).** HYPOTHESIS ONLY, no test. Solo table 895591016, move 69, filed 2026-08-07 15:51 (16 minutes after #236913, same reporter Daravon): playing Bjorn, "There are multiple targets near me, but the Attack button is greyed out". Same deploy window and same subsystem as the two above; unknown whether it shares their cause. Reporter does not know how they got there ("Not sure").
- [ ] **Monster left alive at 0 health after the Queen of the Dead dies (BGA #236814).** OPEN RULES QUESTION for Victoria - no ruling exists, so nothing may be asserted to the reporter. 3p table 894513383, move 290: reporter leaves a Queen-boosted monster at 1 health (move 285), kills the Queen (move 290), the monster loses the +1 and sits at 0 health still on the board ("After reload, it shows 0 but is standing there"). The rulebook supports the reporter only for the ATTACK step - [RULES.md:303](RULES.md#L303) "If a monster has as much damage as its health, you killed it; remove the monster tile and gain as much experience (yellow) as it is worth" - and says nothing about re-checking when a buff is removed retroactively. The BGG thread on exactly this case, [FORUM.md:5834](FORUM.md#L5834) "Question about the Queen of the Dead's +1 health buff" question C, has NO designer answer: both replies are ordinary players agreeing with each other, neither carries the `DESIGNER - confirmed` marker. So this needs a designer ruling, not our own call. ACTION TAKEN 2026-08-12: player-facing comment posted (status left Open, verified by reload) asking the reporter to raise the question on the BGG forum so the designer can answer, and to ask in the same post who gains the XP for a monster that dies this way. Implement once there is a confirmed answer; do not guess in the meantime.



## Tracked bugs - posponeded
- [x] **Optional step-by-step move mode for heroes without a step incentive (feature request, BGA #235315).** IMPLEMENTED 2026-08-16, not yet deployed: player preference 102 "Confirm end of movement" (`Material::MA_PREF_CONFIRM_MOVE`, default off) ORs into `Op_actionMove::hasStepIncentive`, so step mode - hex-by-hex clicks, remaining-step count in the prompt, explicit "End Move" - applies to every move of that player. Wrecking Ball still wins (it owns its own move loop). Flip #235315 on the tracker once shipped. (Distinct from the #234817 step-mode-ends-early BUG above, which is real.)
- [ ] **Step-by-step move: reporter says it ends early on enemy-adjacency / quest-completion (BGA #234817).** Info-needed after investigation. Background agent PROVED server-side NOT A BUG for both sub-claims (green tests `Op_moveStepPrematureEndBug234817Test`, drives the real Op_moveStep loop end-to-end): (1) `HexMap::getReachableHexes` returns the free hexes around an adjacent monster (only excludes the occupied hex), so moveStep keeps prompting - it auto-ends only when the hero is genuinely boxed in (correct per RULES.md:61); (2) completing a TStep quest mid-move (Belt of Youth) does NOT void the moveStep re-queue - loop continues. The prior `HeroMoveAdjacentMonsterBug234817Test` (commit 758a6ad) is a FALSE GUARD (only calls getReachableHexes in isolation, never drives the loop / isTrivial auto-end). IF the reporter's symptom is real with free hexes remaining, it is a CLIENT (TypeScript) issue: the server enters an interactive prompt and never auto-ends while free hexes exist; the step prompt is rendered by the generic handler in `src/GameMachine.ts`. Client-side UNCONFIRMED - needs a live browser repro (chrome-devtools-mcp vs Studio). Asked reporter for a concrete case (open hex available but move still ended) to separate boxed-in (correct) from a client bug.

## Triage run log

Short log of each run: date, reports touched, outcome.

- **2026-08-17** - Two new reports, both from **Lord Aethan (the designer)** on his solo table 900570224: #238473 (own hex not counted as adjacent terrain - rules-confirmed vs RULES.md:412, root cause `countAdjMountains`, hits Dvalin's Pick + Mining Equipment) and #238475 (couldn't use the Wrecking Ball ram; wants step-by-step movement as the norm - overlaps undeployed 6e4a90a/6f1fc02 and open #235051/#235315). Per Victoria's standing rule NO comments or status changes on designer reports - both recorded above, awaiting her call. Studio error log for the table: clean (short name `fate` verified). No deploy-sweep flips due: latest tag is still v260816-1201; the #236177 armor fix (d2c6582) and #235315 preference (6f1fc02) remain untagged, so those reports stay as they are. Marker bumped.

- **2026-08-16** - Deploy sweep for tag **v260816-0944** (ships 5c41929: Eagle Eye strength 1/2 restored in the CSV, Eagle Eye II ignore-armor implemented as `canIgnoreArmor`). #237686 flipped Open -> **Fixed** with the standard template comment, verified by reload; entry removed from Tracked bugs. The fix went through the full loop: red reproducer first (`Campaign_BjornAbilityTest`, 4 tests), blind review clean (verified monsters cannot trigger the bypass, no cross-hero leak, Boldur armor intact), f7b70ae-era pinning test flipped. A note was left on the #236177 entry: when armor moves to the shared deal-damage path, the `canIgnoreArmor` consult must move with it. Still open: #236791, #236916, #236177, #236519, #236178, #234817, #235051, #236814 (awaiting designer ruling).

- previous history removed (in git if needed)